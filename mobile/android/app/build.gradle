plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
    // Push-сповіщення (firebase_messaging) — читає google-services.json поруч.
    id "com.google.gms.google-services"
}

// Стабільний підпис релізу — обов'язковий, інакше кожна збірка (локальна
// чи CI, де дебаг-keystore щоразу генерується наново на чистій машині)
// має СВІЙ сертифікат, і Android відмовляється ставити оновлення поверх
// старого (INSTALL_FAILED_UPDATE_INCOMPATIBLE — саме "конфліктує з іншим
// пакетом"). key.properties в git не потрапляє (.gitignore); локально
// його не буде — тоді просто збираємось під дебаг-ключем, як і раніше.
def keystoreProperties = new Properties()
def keystorePropertiesFile = rootProject.file('key.properties')
if (keystorePropertiesFile.exists()) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
}

android {
    namespace = "net.monsory.monsory_connect"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "net.monsory.monsory_connect"
        // You can update the following values to match your application needs.
        // For more information, see: https://flutter.dev/to/review-gradle-config.
        // Голосові повідомлення (плагін record) вимагають minSdk 23
        // (Android 6.0+, 2015) — вище за стандартний flutter.minSdkVersion.
        minSdk = 23
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    signingConfigs {
        if (keystorePropertiesFile.exists()) {
            release {
                keyAlias = keystoreProperties['keyAlias']
                keyPassword = keystoreProperties['keyPassword']
                storeFile = file(keystoreProperties['storeFile'])
                storePassword = keystoreProperties['storePassword']
            }
        }
    }

    buildTypes {
        release {
            // Є key.properties (CI чи локально з ключем) — підписуємо
            // стабільним релізним ключем. Немає — падаємо назад на дебаг,
            // щоб `flutter run --release` й далі працював без налаштування.
            signingConfig = keystorePropertiesFile.exists() ? signingConfigs.release : signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
